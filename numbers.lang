func main(){
	var a int = 2
	var b int = 3

	print a+b //prints 5

	var a_l vec<int> = [1 2 3]
	//var b_l vec<float> = float(a_l) //by default, any operation that can be done to an individual element can be done to a vec of them, just map(vec, function)
}
